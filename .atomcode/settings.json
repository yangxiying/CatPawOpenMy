{
  "hooks": {
    "PostToolUse": {
      "format-md": {
        "command": "prettier --write",
        "files": ["**/*.md"]
      },
      "format-js": {
        "command": "cd nodejs && npx prettier --write",
        "files": ["nodejs/src/**/*.js"]
      },
      "format-ts": {
        "command": "cd CatPlayer/app && npx prettier --write",
        "files": ["CatPlayer/app/src/**/*.ts", "CatPlayer/app/src/**/*.tsx"]
      },
      "eslint-js": {
        "command": "cd nodejs && npx eslint --fix",
        "files": ["nodejs/src/**/*.js"]
      },
      "validate-spider-meta": {
        "command": "cd nodejs && node scripts/validate-spider-meta.mjs",
        "files": ["nodejs/src/spider/**/*.js"]
      }
    },
    "PreToolUse": {
      "block-lock-files": {
        "block": true,
        "files": [
          "**/package-lock.json",
          "**/yarn.lock",
          "**/pnpm-lock.yaml"
        ],
        "message": "锁定文件不应由 AI 直接修改。请手动执行 npm install / yarn / pnpm install 来更新。"
      },
      "block-db-json": {
        "block": true,
        "files": ["**/db.json"],
        "message": "db.json 不应提交到仓库。设置 NODE_PATH 环境变量可将持久化数据写入临时目录。"
      }
    }
  },
  "permissions": {
    "allow": [
      "Edit",
      "Write",
      "Bash(cd nodejs && npm run dev:*)",
      "Bash(cd nodejs && npm run build:*)",
      "Bash(npx skills *)"
    ]
  }
}
